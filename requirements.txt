# hapticf env — Python 3.10, CUDA 13.0
#
# Install PyTorch first (cu130 build, README step 3), then this file.
# Install isolated from ~/.local (PYTHONNOUSERSITE=1), or CUDA libs collide.

# --- Core scientific stack ---
# numpy is capped: mediapipe and open3d break on 2.x, and there is no 1.25-1.27
# (1.26.4 -> 2.0.0), so this cap is the top of the usable range.
numpy<=1.24.4
scipy<=1.10.1
pandas>=2.3.3
matplotlib>=3.10.9
PyYAML>=6.0.3

# --- Computer vision / tactile sensing ---
opencv-python>=4.11.0
open3d
ml_collections>=0.1.1
six

# --- Gripper (Modbus RTU over RS-485) ---
# pyrobotiqgripper installs from src/ (README step 3) and needs v3.x, which is
# pymodbus-based. It declares numpy>=1.28, so pip warns against the cap above —
# ignore it; the library only uses long-stable numpy APIs.
pymodbus>=3.12.1
pyserial>=3.5

# --- Haptic / ESP32 ---
mpremote>=1.28.0
esptool>=5.3.1

# --- Hand tracking (cap keeps protobuf/jax/numpy compatible) ---
mediapipe<=0.10.11

# --- Keyboard input ---
pynput>=1.8.2
